# Task 5 — Карты: объекты и атмосфера — отчёт

## Коммиты
- `507dc55` shturm: карты с объектами и атмосферой 🗺️ (mapsVisual.ts перепил + туман main.tsx)
- `44f1de3` shturm: карты фикс грунта над базой, крона светлее 🗺️

## Что сделано (только `src/three/mapsVisual.ts` + 2 строки тумана в `main.tsx`, sim не тронут)
- ДВОР: забор-доски с щелями (alphaTest-текстура канваса) + столбы по периметру,
  будка, покрышки-баррикады (торы), ящики, лужи-глянец (roughness 0.1, envMapIntensity 1.5).
- ОСТРОВ: песок с рябью (sand map repeat 10), 5 пальм (ствол + 6 листьев кроны + кокосы),
  6 камней-додекаэдров, вода-кольцо по краю (opacity 0.8, скролл текстуры через onBeforeRender).
- НЕОН: мокрый асфальт (roughness 0.4, metalness 0.2), 3 билборда с emissive sign-текстурой,
  трубы вдоль стен (rust), 3 голограммы-аддитив (opacity 0.25, парение через onBeforeRender).
- Статика merged по материалам (2–5 мешей на карту + фонари как раньше), коллизии без изменений.
- Туман: island far 110→90, neon near 12→8 / far 70→55 (scene.fog остаётся THREE.Fog).
- Сигнатуры `buildMapVisual` / `disposeMapVisual` без изменений; dispose чистит локальные
  текстуры забора/воды (общий кэш getTex не трогает).

## Тесты и build
- `bun test`: 7 pass / 0 fail (player, waves, weapons, textures, rig — новых тестов не требовалось).
- `bun run build`: зелёный (vite, без ошибок TS).

## Браузер (свой preview localhost:4176, чужой :4175 не трогал)
- Каждая карта: меню→`__shturm.start(map,'normal')`→бой с `aimNearest+fire` 5–11 с.
  Консоль: 0 ошибок на всех трёх картах.
- FPS (headless swiftshader, шумный): yard 27–52, island 29–39, neon 24–26.
  База старого кода (d0c8ee6, отдельный стенд :4177): yard 34–40 — регрессии нет.

## Скрины (`shturm.bratuxa.zomb.top/shots/`, не коммитить)
- task5-yard.png — забор, будка, покрышки, ящики, лужи
- task5-island.png — песок, пальмы, камни, вода; task5-island-third.png — доп. ракурс
- task5-neon.png — билборды, голограммы, трубы, мокрый асфальт

## Concerns
1. `scene.ts` кладёт вечнозелёный грунт 90×90 на y=0 — перекрывал землю карт (остров/неон
   были невидимы). Обход: грунты карт подняты на y=0.05 (лужи 0.07, вода 0.09).
   Правильный фикс — убрать/перекрасить базу в scene.ts (вне скоупа Task 5).
2. HUD показывает «HP NaN» — предсуществующий баг (не Task 5, sim/HUD не трогал).
3. Неон тёмный для чтения без bloom — ждём Task 8 (bloom вывесок).
4. FPS в headless-низкий и шумный (swiftshader); на HW-рендере нужен повторный замер в Task 8.
5. Замер 8 FPS в одном прогоне — артефакт: две живые вкладки делили CPU; на чистой странице 39–52.
