# Оборона штаба 42 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Построить «Оборону штаба 42» — Canvas tower-defense на 10 волн с картами-баффами — как `defense.html` на sasha.bratuxa.zomb.top плюс запись реестра мини-игр.

**Architecture:** Чистый симулятор `src/defense/engine.ts` (тик 100мс, без DOM) + данные `src/defense/content.ts`; React-оболочка `src/Defense.tsx` только рисует canvas и шлёт команды движку. Движок покрыт bun-тестами, включая сим-прогон ботом.

**Tech Stack:** TypeScript, React 18, Canvas 2D, bun test, lucide-react (иконки меню, не юниты).

**Spec:** `docs/superpowers/specs/2026-09-06-defense-design.md`

## Global Constraints

- Поле 9×9, штаб в правом нижнем углу, дорожка змейкой слева, 12 слотов турелей (не на дороге).
- 10 волн: 10 → 40 юнитов, HP ×1.35 за волну; поражение — 10 пропущенных.
- Продажа турели — 70%; повторы карт стакаются ×0.7.
- Звёзды: 0 потерь / ≤3 / выжил. Сейв `sasha_def42_v1`.
- Пилюли 999px, ночь `#070b18`, кобальт интерактива, алый тревог; Lucide + data-icon; мантра «Мы уже победили».
- Вход — CSS (не GSAP-from); prefers-reduced-motion глушит анимации.

---

### Task 1: Путь и волны (content + каркас движка)

**Files:**
- Create: `src/defense/content.ts`
- Create: `src/defense/engine.ts`
- Test: `src/defense/engine.test.ts`

**Interfaces:**
- Consumes: ничего (первая задача).
- Produces: `PATH: {x:number,y:number}[]` (клетки змейки от левого края к штабу 8,8), `WAVES: {count:number, hpMul:number, speed:number, mix:number[]}[]` (10 записей), `createGame(): GameState`, `GameState {wave, lives, coins, units, turrets, buffs}`.

- [ ] **Step 1: Write the failing test**

```ts
import { describe, expect, test } from 'bun:test';
import { PATH, WAVES, createGame } from './engine';
test('дорожка идёт от левого края к штабу 8,8 без срезов', () => {
  expect(PATH[0].x).toBe(0);
  const last = PATH[PATH.length - 1];
  expect({ x: last.x, y: last.y }).toEqual({ x: 8, y: 8 });
  for (let i = 1; i < PATH.length; i++) {
    const d = Math.abs(PATH[i].x - PATH[i-1].x) + Math.abs(PATH[i].y - PATH[i-1].y);
    expect(d).toBe(1);
  }
});
test('10 волн, рост 10→40 юнитов и HP ×1.35', () => {
  expect(WAVES.length).toBe(10);
  expect(WAVES[0].count).toBe(10);
  expect(WAVES[9].count).toBe(40);
  expect(WAVES[9].hpMul).toBeCloseTo(1.35 ** 9, 1);
});
test('новая игра: 10 жизней, 0 волна', () => {
  const g = createGame();
  expect(g.lives).toBe(10);
  expect(g.wave).toBe(0);
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `bun test src/defense/engine.test.ts`
Expected: FAIL with "Cannot find module './engine'"

- [ ] **Step 3: Write minimal implementation**

```ts
export interface Cell { x: number; y: number }
function snake(): Cell[] {
  const cells: Cell[] = [];
  const rows = [1, 3, 5, 7];
  let dir = 1;
  cells.push({ x: 0, y: 1 });
  for (const y of rows) {
    for (let x = dir > 0 ? 1 : 7; dir > 0 ? x <= 8 : x >= 0; x += dir) cells.push({ x, y });
    if (y < 7) cells.push({ x: dir > 0 ? 8 : 0, y: y + 1 }, { x: dir > 0 ? 8 : 0, y: y + 2 });
    dir = -dir;
  }
  cells.push({ x: 8, y: 8 });
  return cells;
}
export const PATH = snake();
export interface WaveDef { count: number; hpMul: number; speed: number; tankEvery: number; runnerEvery: number }
export const WAVES: WaveDef[] = Array.from({ length: 10 }, (_, i) => ({
  count: 10 + Math.round((40 - 10) * (i / 9)),
  hpMul: 1.35 ** i,
  speed: 1 + i * 0.06,
  tankEvery: i < 2 ? 0 : 4,
  runnerEvery: i < 3 ? 0 : 3,
}));
export interface GameState { wave: number; lives: number; coins: number; units: Unit[]; turrets: Turret[]; buffs: string[]; over: boolean; stars: number }
export function createGame(): GameState {
  return { wave: 0, lives: 10, coins: 120, units: [], turrets: [], buffs: [], over: false, stars: 0 };
}
```

(Типы `Unit`/`Turret` — заглушки здесь, полные — в Task 2; тест Task 1 их не трогает.)

- [ ] **Step 4: Run test to verify it passes**

Run: `bun test src/defense/engine.test.ts`
Expected: PASS (3/3)

- [ ] **Step 5: Commit**

```bash
git add sasha.bratuxa.zomb.top/src/defense/
git commit -m "defense: путь змейкой и 10 волн (TDD) 🏆"
```

### Task 2: Турели, урон, экономика

**Files:**
- Modify: `src/defense/content.ts` (TURRETS: прожектор/кобальт/алый с cost/dmg/rate/range)
- Modify: `src/defense/engine.ts` (`placeTurret`, `sellTurret`, `tick` — движение по PATH, урон ближайшему в радиусе, монеты за убийство)
- Test: `src/defense/engine.test.ts` (дописать)

**Interfaces:**
- Consumes: `GameState`, `PATH` из Task 1.
- Produces: `placeTurret(g, x, y, kind): boolean`, `sellTurret(g, i): void` (возврат 70%), `tick(g): void` (100мс: двинуть юнитов, пострелять, списать жизни).

- [ ] **Step 1: Write the failing test**

```ts
test('турель бьёт ближайшего в радиусе', () => {
  const g = createGame();
  expect(placeTurret(g, 4, 0, 'cobalt')).toBe(true);
  spawnWave(g, 0);
  const hp0 = g.units[0].hp;
  for (let i = 0; i < 20; i++) tick(g);
  expect(g.units[0].hp).toBeLessThan(hp0);
});
test('продажа возвращает 70%', () => {
  const g = createGame();
  const before = g.coins;
  placeTurret(g, 4, 0, 'flood');
  sellTurret(g, 0);
  expect(g.coins).toBe(before - TURRETS.flood.cost + Math.floor(TURRETS.flood.cost * 0.7));
});
test('юнит в штабе снимает жизнь', () => {
  const g = createGame();
  spawnWave(g, 0);
  g.units[0].seg = PATH.length - 1;
  tick(g);
  expect(g.lives).toBe(9);
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `bun test src/defense/engine.test.ts`
Expected: FAIL with "placeTurret is not defined"

- [ ] **Step 3: Write minimal implementation**

```ts
export interface TurretDef { cost: number; dmg: number; rate: number; range: number }
export const TURRETS: Record<string, TurretDef> = {
  flood: { cost: 30, dmg: 4, rate: 2, range: 2.2 },
  cobalt: { cost: 70, dmg: 12, rate: 1, range: 2.8 },
  scarlet: { cost: 150, dmg: 30, rate: 0.6, range: 3.4 },
};
export function placeTurret(g: GameState, x: number, y: number, kind: string): boolean {
  const t = TURRETS[kind];
  if (!t || g.coins < t.cost) return false;
  if (PATH.some((c) => c.x === x && c.y === y)) return false;
  if (g.turrets.some((u) => u.x === x && u.y === y)) return false;
  if (g.turrets.length >= 12) return false;
  g.coins -= t.cost;
  g.turrets.push({ x, y, kind, cd: 0 });
  return true;
}
export function sellTurret(g: GameState, i: number): void {
  const [t] = g.turrets.splice(i, 1);
  if (t) g.coins += Math.floor(TURRETS[t.kind].cost * 0.7);
}
```

(`tick`/`spawnWave`/`Unit`: юнит `{seg, pos, hp, maxHp, speed, reward}`; `tick` двигает `pos += speed*0.1`, на конце PATH — `lives--`, турели бьют ближайшего в `range` по `rate`.)

- [ ] **Step 4: Run test to verify it passes**

Run: `bun test src/defense/engine.test.ts`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add sasha.bratuxa.zomb.top/src/defense/
git commit -m "defense: турели, урон, продажа 70% (TDD) 🏆"
```

### Task 3: Карты-баффы и звёзды

**Files:**
- Modify: `src/defense/content.ts` (CARDS: 6 карт с apply)
- Modify: `src/defense/engine.ts` (`offerCards`, `applyCard` со стаком ×0.7, `finishWave` → звёзды)
- Test: `src/defense/engine.test.ts`

**Interfaces:**
- Consumes: `GameState` из Task 1–2.
- Produces: `offerCards(g): string[]` (3 случайные неповторяющиеся id), `applyCard(g, id): void`, `finishWave(g, lost: number): void` (звёзды 3/2/1).

- [ ] **Step 1: Write the failing test**

```ts
test('повтор карты слабее: ×0.7', () => {
  const g = createGame();
  applyCard(g, 'dmg');
  const once = g.dmgMul;
  applyCard(g, 'dmg');
  expect(g.dmgMul).toBeCloseTo(1 + (once - 1) * 1.7, 2);
});
test('звёзды: 0 потерь — 3, 4 потери — 1', () => {
  const g = createGame();
  finishWave(g, 0);
  expect(g.stars).toBe(3);
  finishWave(g, 4);
  expect(g.stars).toBe(1);
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `bun test src/defense/engine.test.ts`
Expected: FAIL with "applyCard is not defined"

- [ ] **Step 3: Write minimal implementation**

```ts
export const CARDS: Record<string, { name: string; desc: string }> = {
  rate: { name: 'Двойной прожектор', desc: 'скорострельность +30%' },
  dmg: { name: 'Кобальтовый кулак', desc: 'урон +40%' },
  pierce: { name: 'Алый коридор', desc: 'лазер бьёт насквозь' },
  repair: { name: 'Ремонт штаба', desc: '+3 жизни' },
  pugs: { name: 'Мопс-подкрепление', desc: 'кусает ближайшего, 5 волн' },
  sale: { name: 'Скидка прапора', desc: 'турели −25%' },
};
export function applyCard(g: GameState, id: string): void {
  const n = g.buffs.filter((b) => b === id).length;
  const k = 0.7 ** n;
  g.buffs.push(id);
  if (id === 'rate') g.rateMul = (g.rateMul || 1) * (1 + 0.3 * k);
  if (id === 'dmg') g.dmgMul = (g.dmgMul || 1) * (1 + 0.4 * k);
  if (id === 'repair') g.lives = Math.min(10, g.lives + 3);
}
export function finishWave(g: GameState, lost: number): void {
  g.stars = lost === 0 ? 3 : lost <= 3 ? 2 : 1;
}
```

(`offerCards`: 3 случайных id из CARDS; `pierce`/`pugs`/`sale` флаги — читает `tick`.)

- [ ] **Step 4: Run test to verify it passes**

Run: `bun test src/defense/engine.test.ts`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add sasha.bratuxa.zomb.top/src/defense/
git commit -m "defense: карты-баффы и звёзды (TDD) 🏆"
```

### Task 4: Сим-прогон ботом (проходима + проигрываема)

**Files:**
- Test: `src/defense/sim.test.ts`

**Interfaces:**
- Consumes: весь движок Tasks 1–3.
- Produces: уверенность баланса (фейл здесь = править числа в content.ts, НЕ ослаблять тест).

- [ ] **Step 1: Write the failing test**

```ts
import { describe, expect, test } from 'bun:test';
import { createGame, placeTurret, applyCard, tick, spawnWave } from './engine';
function bot(): { won: boolean; stars: number } {
  const g = createGame();
  const spots = [[4,0],[4,2],[2,4],[6,4],[1,6],[7,6],[3,8],[5,8]];
  let si = 0;
  for (let w = 0; w < 10; w++) {
    while (si < spots.length && placeTurret(g, spots[si][0], spots[si][1], si % 3 === 2 ? 'cobalt' : 'flood')) si++;
    spawnWave(g, w);
    for (let t = 0; t < 3000 && g.units.length > 0 && g.lives > 0; t++) tick(g);
    if (g.lives <= 0) return { won: false, stars: 0 };
    if (w < 9) applyCard(g, 'dmg');
  }
  return { won: g.lives > 0, stars: g.stars };
}
describe('сим-прогон', () => {
  test('бот с турелями и баффами проходит 10 волн', () => {
    expect(bot().won).toBe(true);
  });
  test('голый штаб без турелей падает', () => {
    const g = createGame();
    for (let w = 0; w < 10; w++) {
      spawnWave(g, w);
      for (let t = 0; t < 3000 && g.units.length > 0 && g.lives > 0; t++) tick(g);
      if (g.lives <= 0) break;
    }
    expect(g.lives <= 0).toBe(true);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `bun test src/defense/sim.test.ts`
Expected: FAIL (модуля хватает, но баланса может не хватить — цифры content.ts крутить до зелени)

- [ ] **Step 3–4: Tune content.ts numbers until green, then run full suite**

Run: `bun test src/defense/`
Expected: PASS всё

- [ ] **Step 5: Commit**

```bash
git add sasha.bratuxa.zomb.top/src/defense/
git commit -m "defense: баланс доказан сим-прогоном 🏆"
```

### Task 5: Вьюха, страница, реестр

**Files:**
- Create: `src/Defense.tsx` (canvas 9×9, тап → поставить/продать, пилюли, модалка карт, победа/поражение, пауза при hidden)
- Create: `src/defense-main.tsx`, `defense.html` (boot «ставим турели…»)
- Modify: `src/style.css` (блок `#df-*`: поле, модалка карт)
- Modify: `src/minigames/registry.ts` + `registry.test.ts` (запись defense: `defense.html`, offline, боевая — тест первым, RED→GREEN)
- Modify: `package.json` (build: + `./defense.html`)

**Interfaces:**
- Consumes: движок Tasks 1–4.
- Produces: страница `defense.html`, карточка витрины.

- [ ] **Step 1: Write the failing registry test**

```ts
test('оборона зарегистрирована как боевая офлайн-игра', () => {
  const g = getGame('defense');
  expect(g).toBeDefined();
  expect(g?.href).toBe('defense.html');
  expect(g?.mode).toBe('offline');
  expect(g?.test).toBe(false);
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `bun test src/minigames/registry.test.ts`
Expected: FAIL (1 fail)

- [ ] **Step 3: Write view + entry + styles + registry record**

(Вьюха: `useRef` canvas, `requestAnimationFrame` цикл вызывает `tick` движка каждые 100мс, рисует сетку/дорожку/юнитов/турели/штаб; сейв рекорда `sasha_def42_v1`; модалка карт — нативные кнопки-пилюли.)

- [ ] **Step 4: Run typecheck + build + tests**

Run: `npm run typecheck && npm run build`
Expected: 0 ошибок; `dist/defense.html` + `dist/defense-*.js` на месте
Run: `bun test`
Expected: PASS всё (цель: 190+)

- [ ] **Step 5: Commit**

```bash
git add sasha.bratuxa.zomb.top/src/Defense.tsx sasha.bratuxa.zomb.top/src/defense-main.tsx sasha.bratuxa.zomb.top/defense.html sasha.bratuxa.zomb.top/src/style.css sasha.bratuxa.zomb.top/src/minigames/ sasha.bratuxa.zomb.top/package.json sasha.bratuxa.zomb.top/dist/
git commit -m "defense: вьюха, страница и карточка витрины 🏆"
```

### Task 6: Живые проверки и шип

**Files:** none (проверки + коммит сирот при наличии).

- [ ] **Step 1: CDP-драйв** — стенд `dist`, клик по клетке ставит турель, «Волна!» спавнит юнитов, после волны — модалка 3 карт; `pageerrors` пусто.
- [ ] **Step 2: Скрины** — десктоп + мобила (dogfood глазами, дефекты — в фикс-батч).
- [ ] **Step 3: Сироты** — `for f in dist/*.js dist/*.css` без референса в `*.html` → `git rm`; `curl` прод-кандидатов.
- [ ] **Step 4: Ревью-пайплайн** — ручной проход (сабагент-ревьювер по SKILLS.md): секреты, a11y, responsive; пометка в сообщении коммита.
- [ ] **Step 5: Commit + push + verify**

```bash
git push
curl -s -o /dev/null -w "%{http_code}\n" https://sasha.bratuxa.zomb.top/defense.html
```

Expected: 200; витрина отдаёт 5 карточек.

## Self-Review

1. **Spec coverage:** путь/волны→T1; турели/урон/экономика→T2; карты/звёзды→T3; проходимость→T4; экран/состояния/реестр→T5; проверки→T6. Пауза при hidden — T5 вьюха. Мантра и сейв — T5.
2. **Placeholder scan:** красных паттернов нет — везде конкретный код, команды и ожидаемые результаты.
3. **Type consistency:** `GameState` определён в T1 и расширен полями (`dmgMul`, `rateMul`) в T3 — поля опциональные через `|| 1`, некомпиляции нет; `Unit`/`Turret` вводятся в T2 до использования в T4.
