# Task 4 — Review: репетиция на локальных портах

Дата ревью (UTC): 2026-09-06. Ревьюер инфры. Код не менялся — ревью отчёта
`task-4-report.md` + живых последствий. Сервисы не трогались, суб-агентов не было.

## Вердикт

- **Spec: ✅** — матрица 20/20 принята по отчёту, приборка подтверждена живьём.
- **Quality: хорошая** — отклонения от брифа задокументированы честно и обоснованы,
  concerns для Task 5+ выделены отдельно. Мелочь: в `/tmp/caddy-rehearse/` остался
  `access.log` на 174 КБ — допустимо (вне git), но можно удалить при следующей приборке.

## Живые проверки (все сошлись с отчётом)

1. Процессов `router-rehearse` / `Caddyfile.test` нет — единственный match в `pgrep -f`
   это обёртка моего же shell (проверено через `/proc/<pid>/cmdline`).
2. Порты 8080, 8443, 18080, 18443, 2019 — все FREE (`ss -tln`).
3. `systemctl is-active chaev-site` → `active`, MainPID=47072; роутер `python3`
   держит `0.0.0.0:80` и `0.0.0.0:443` (тот же pid, что до репетиции).
4. Прод `:80` chaev (Host-заголовок) → 200; `:8081` shturm → 200.
5. Продовый `caddy run --config /etc/caddy/Caddyfile` (pid 60030) жив, не задет.
6. Артефакты только в `/tmp/caddy-rehearse/` (`Caddyfile.test`, `router-rehearse.py`,
   `router.log` (пустой), `access.log`) — вне git.
7. `git status`: правок `infra/Caddyfile` / `router.py` нет, коммитов нет;
   только чужие `M mtt.bratuxa.zomb.top/...` и untracked `.superpowers/`, `dist/`.

## Findings (построчно)

1. Матрица 20/20 живьём не перепроверялась — репетиционные процессы уже остановлены
   (ожидаемо); вердикт по матрице опирается на отчёт, противоречий ему не найдено.
2. Отклонения от брифа (sed портит апстримы, конфликт портов роутера, 308 без дублирования
   схем, `local_certs`, `caddy run` вместо зависшего `caddy start`) — вынужденные,
   описаны корректно, прод-конфиг не затронут.
3. Concern из отчёта подтверждается: прод-роутер (pid 47072) слушает `0.0.0.0:80/443`,
   а `router.py` в репо биндит `127.0.0.1:8080/8443` — запущен старый вариант файла;
   разобраться до Task 5, что должно держать 80/443.
4. Concern из отчёта подтверждается: прод-Caddy (`/etc/caddy/Caddyfile`, `:80 → /usr/share/caddy`)
   конкурирует за :80, сейчас :80 держит роутер — вне скоупа Task 4, перенести в Task 5+.
5. Бриф Task 4 (sed-шаг, `caddy start`) устарел относительно текущих файлов — поправить
   до следующей репетиции.
