# Task 5 review — Ночь X (верификация живьём, UTC 2026-09-06 ~01:0x)

## Вердикт

- **Spec: ✅ PASS** — Caddy на :80/:443, тыл на :8080, матрица 20/20 × 200 по HTTPS, браузер-спотчек чистый.
- **Quality: хорошо, 1 некритичное замечание** (отчёт пишет неверный домен — см. finding F1).

## Проверено живьём (сервисы не трогали, рестартов не было)

- Сервисы: `chaev-site` active/running (python3 pid 68497), `caddy` active/running (pid 68793).
- Порты (ss): `:80`/`:443` → caddy; `127.0.0.1:8080`+`:8443` → python3-тыл; `127.0.0.1:8081` → docker-proxy (shturm); `:8091–8095` → Bun/API живы.
- curl-матрица 20/20 × 200 (SNI-корректно, через loopback): 1b42p, 42laiv, 42vs, ai-714ef0, brohacho, chaev, denis, doom, evaelph, gtaevv, hub, miqqil, mtt, pampers, sasha, setden, skitons, smolgrad, svyatoslav, shturm.
- Strict-TLS без -k: `chaev → 200, ssl_verify_result=0`; серт Let's Encrypt YE2, `notAfter Dec 4 2026`.
- Браузер через Caddy-HTTPS: shturm — меню рендерится (ШТУРМ-43, карты, «В бой!»), клик → canvas 780×437 visible; ресурсов 5. Hub (сосед) — title «🐴 ИГРЫ 1Б42П GAMES 🏆», body ~19k, ресурсов 6. Рендер-ломающих JS-ошибок нет.
- Отклонение stop→restart соответствует плану 96c5980 — принято, бриф Step 1 устарел (stop убил бы тыл :8080).

## Findings (построчно)

- **F1 (отчёт врёт в детали, прод ОК):** отчёт пишет матрицу по `https://$d.1b42p.ru/` — живой Caddyfile держит только `*.bratuxa.zomb.top` (что совпадает с брифом Task 5). Матрица перепроверена на правильном домене — 20/20 × 200. Поправить отчёт, иначе wtf при аудите.
- **F2 (окружение проверки, не прод):** локальный резолв public-имён мёртв (NXDOMAIN, systemd-resolved stub) — curl шёл через `--resolve …:127.0.0.1`, браузер — через временные записи /etc/hosts (после проверки убраны, файл возвращён к исходному). На прод-трафик не влияет, но учесть для будущих ночных проверок.
- **F3 (косметика, доб-cutover):** `favicon.ico → 404` на hub (подтверждено) и shturm — отдаёт тыл-роутер, как и было. Не JS-ошибка, на рендер не влияет. Можно добить отдельным таском (отдать favicon из Caddy).
- **F4 (не finding, для протокола):** `transferSize==0` в performance-ресурсах — это кеш, не битые; моя проба `/gsap.min.js` → 404 — неверный путь (реально грузится с cdnjs). Прод чист.
- **F5 (лимит харнеса, совпадает с отчётом):** полный перехват консоли невозможен — CDP/js-promise виснет при активном game-loop; оценка «0 ошибок» — по факту рендера + ресурсов, как и в отчёте.

## Матрица кратко

20/20 × 200 по `https://<host>.bratuxa.zomb.top/` (strict TLS, LE до 2026-12-04): все хосты из брифа — 200. Исключений нет; единственный не-200 в округе — `favicon.ico` 404 (F3, косметика).
