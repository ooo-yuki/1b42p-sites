# Task 4 — Репетиция на локальных портах: DONE (20/20)

Дата (UTC): 2026-09-06. Прод НЕ трогали: `chaev-site` active, роутер pid 47072 на :80/:443 жив, `caddy run` pid 60030 жив, контейнер shturm :8081 жив.

## Матрица (curl Host-заголовки → http://127.0.0.1:8080/, через тестовый Caddy)

| host | code | host | code |
|---|---|---|---|
| 1b42p | 200 | miqqil | 200 |
| 42laiv | 200 | mtt | 200 |
| 42vs | 200 | pampers | 200 |
| ai-714ef0 | 200 | sasha | 200 |
| brohacho | 200 | setden | 200 |
| chaev | 200 | skitons | 200 |
| denis | 200 | smolgrad | 200 |
| doom | 200 | svyatoslav | 200 |
| evaelph | 200 | shturm | 200 |
| gtaevv | 200 | hub | 200 |

Итого: **20 строк, 20×200, 0 не-200.** Бонус: `https://:8443` с правильным SNI — chaev 200, shturm 200 (local CA, `curl -k --resolve`).

## Отклонения от брифа (вынужденные, иначе репетиция невозможна)

Бриф дословно невыполним с текущими `infra/Caddyfile` + `router.py`, проверено:
1. `sed 's/:80/:8080/'` портит апстримы: `:8081`→`:808081`, `:8080`→`:808080` (в Caddyfile нет `:80`/`:443` как адресов сайтов). Тестовый Caddyfile собран вручную: глобально `http_port 8080` + `https_port 8443`, апстримы не тронуты.
2. Временный роутер (`router.py`) слушает 127.0.0.1:8080+8443 — те же порты, что хотел занять тестовый Caddy. Тыл запущен с копии `/tmp/caddy-rehearse/router-rehearse.py` (diff к оригиналу — только 8080→18080, 8443→18443), апстрим 19 хостов в Caddyfile.test — `127.0.0.1:18080`. Матричная команда (`curl :8080` с Host) — дословно по брифу.
3. Именованные хосты без `auto_https disable_redirects` отдают по HTTP 308, а с ним Caddy вообще не биндит :8080. Поэтому в Caddyfile.test адреса продублированы со схемами `http://`+`https://` (маршрутизация 1:1 как в проде) — только тогда :8080 слушает и отдаёт 200.
4. `local_certs` в тестовом конфиге — иначе Caddy ходит в реальный ACME/ZeroSSL за 20 доменами (зафиксированы попытки в логе первой попытки). На прод-конфиг не влияет.
5. `caddy start` висел (>80 с, убит по таймауту); вместо него — `caddy run` в фоне. Прод-`caddy run` (:80-дефолт из /etc/caddy, admin :2019 был свободен) не задет. `caddy stop` не выполнялся (убил бы невесть что) — тестовый Caddy остановлен точечно, остатков процессов нет.

## Приборка (проверено)

- :8080/:8443/:18080/:18443/:2019 — свободны; процессов `router-rehearse` / `Caddyfile.test` нет.
- `systemctl is-active chaev-site` → **active**; прод-роутер на :80/:443 (pid 47072, до репетиции) на месте.
- Прод-проверки после приборки: `:80` chaev → 200; `:8081` shturm → 200.
- Артефакты только в `/tmp/caddy-rehearse/` (вне git). Файлы репозитория не правились, коммитов нет (в `git status` только чужие M от других тасок).

## Concerns для Task 5+

1. **Бриф Task 4 устарел**: sed-шаг и `caddy start` опасны/неработоспособны с нынешними файлами — бриф стоит поправить до следующей репетиции.
2. **Прод-роутер (pid 47072) слушает 0.0.0.0:80/443, а текущий `router.py` в репо биндит 127.0.0.1:8080/8443** — запущен старый вариант файла (перезапуск затрет поведение; также `systemctl show` говорит ExecStart именно этот файл). Разобраться до Task 5: что реально должно слушать 80/443 — роутер или Caddy?
3. **Прод-Caddy (`/etc/caddy/Caddyfile`, `:80 → /usr/share/caddy`) конкурирует за :80** — сейчас :80 держит роутер, Caddy прод-состояние не проверялся (не в скоупе Task 4).
4. HTTPS-репетиция валидна только с `local_certs`; продовые LE-серты (`/etc/letsencrypt/.../chaev...`) на месте и использованы тылом-репетицией без ошибок.
