# Task 9 review — React HUD + ввод + тач

Вердикт: Spec ❌ · Quality: средняя · Commit 81c7866 (3 файла, +140/−1) соответствует diff 864a0a5..81c7866.

## Spec-чеk (бриф — single source of truth)
- [x] `src/ui/hud.tsx` — verbatim по брифу: HP/Волна/патроны/слот + `V — 1/3 лицо • 1/2/3 — оружие • R — перезарядка 🏆`. Совпадает дословно.
- [x] `src/ui/App.tsx` — Hud + левый стик (move) + правый стик (look) + кнопки Огонь/Прицел/Перезарядка (тач+мышь) + WASD/1/2/3/R/V. Всё присутствует.
- [x] `src/main.tsx` — рендер `<App/>` в #root + `initScene(canvas #game)`. Связка есть.
- [x] Коммит `shturm: React HUD джойстики 📱` — есть, состав файлов совпадает.
- [ ] V-переключение вида — СЛОМАНО в рантайме (см. F1). Код есть, эффекта нет → пункт спеки не выполнен.

## Findings
- F1 (блокер, Spec): двойной слушатель KeyV обнуляет тоггл. `cameraRig.ts:20-21` (модульный listener, читает `view` напрямую) + `App.tsx:104` (читает `getView()`). Порядок: cameraRig third→first, затем App first→third — итог ноль тогглов за нажатие. Отчёт (§Concerns) ошибочно пишет «итог — один тоггл». Фикс: убрать listener из `cameraRig.ts`, оставить один в App (как сам отчёт и предлагает для Task 10).
- F2 (Global «UI не считает логику» — нарушение, но как заглушка терпимо): `App.tsx:105-108,139` — `setAmmo(AMMO_FULL[...])` на 1/2/3/R/кнопке. UI симулирует патроны вместо отображения состояния сима. Принято только как stub до Task 10; в Task 10 ammo/hp/wave должны прийти из игрового цикла, `AMMO_FULL` из UI — удалить.
- F3 (minor): `SLOTS` (App.tsx:42) объявлен и не используется.
- F4 (minor): `pollKeys` (App.tsx:117-118) — избыточная ветка: обе ветви делают одно и то же присваивание, условие `!('ontouchstart' in window)` дублируется. Упростить до прямого `inputBus.move = {x, y}` + отдельный guard приоритета стика при необходимости.
- F5 (minor): `useEffect(..., [slot])` переподписывает keydown/keyup при каждой смене слота; обработчик R читает `slot` из замыкания (stale между рендерами). Лечится `slotRef` или пустым deps + функциональные setState.
- F6 (minor): кнопки Огонь/Прицел — нет `onMouseLeave`/`onTouchCancel` сброса и `onMouseUp` вне кнопки; флаг может залипнуть в `true`. Правый стик пишет абсолютные `look.dx/dy` без сброса — контракт «цикл сбрасывает» нигде не реализован (отложено на Task 10, ок, но зафиксировать).
- F7 (info): стики только touch-события, мышью на десктопе не потянуть — для десктопа ок (WASD+мышь), но проверить на тач-эмуляторе стоит в Task 10.

## Quality
Средняя: структура чистая (inputBus, Stick-компонент, zIndex/touchAction учтены), typecheck по отчёту чистый (не перепроверялся рантаймом здесь — файловый состав и подписи совпадают). Минусы: F1 — функциональный баг ключевой клавиши; F2 — логика патронов в UI; мелкий мёртвый/запутанный код (F3–F5), залипание флагов (F6).

## Решение
- Spec ❌ до исправления F1 (один слушатель V) — перепроверка чтением `cameraRig.ts` + `App.tsx` достаточна, без рантайма.
- F2 принять как временный stub с обязательным выносом в Task 10; F3–F6 — зачистить в Task 10 или fixup-коммитом.

## Re-review R1 (fix d9573c8, scope: F1 only)
- F1 ADDRESSED: `cameraRig.ts` — модульный `window.addEventListener KeyV` удалён (живьём слушателя нет, файл 17 строк: set/get/update только); единственный тоггл — `App.tsx:77` (`setView(getView()==='first'?'third':'first')`). Фикс-дифф `81c7866..d9573c8`: 1 файл, 6 deletions, только удаление дубля. Двойного срабатывания больше нет.
- F2–F7: out of scope (deferred, без изменений).
- New breakage: no — дифф чистое удаление, других файлов не касается, `setView/getView/updateCamera` API нетронут.
